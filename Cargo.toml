[workspace]
resolver = "2"
members = [
    "crates/xlit-core",
    "crates/xlit-cli",
]

[workspace.package]
edition = "2021"
license = "MIT OR Apache-2.0"
repository = ""

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
