[package]
name = "xlit-learn"
version = "0.1.1"
description = "Per-user learning store: remembers past selections and boosts them."
edition.workspace = true
license.workspace = true

[dependencies]
xlit-core = { path = "../xlit-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
